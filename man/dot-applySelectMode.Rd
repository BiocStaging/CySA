% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server-helpers.R
\name{.applySelectMode}
\alias{.applySelectMode}
\title{Combine an existing selection with a newly picked set of IDs}
\usage{
.applySelectMode(rs, picked, mode)
}
\arguments{
\item{rs}{Integer vector of the currently selected IDs.}

\item{picked}{Integer vector of newly picked IDs.}

\item{mode}{One of \code{"remove others"}, \code{"add"}, \code{"remove"},
or anything else (falls through to \code{picked}). \code{NULL} is
treated as \code{"view"} (falls through to \code{picked}) rather than
erroring in \code{switch()}.}
}
\value{
Integer vector: the combined selection.
}
\description{
Shared "selectMode" semantics used by every plotly click/select observer
in this app. \code{"remove others"} narrows the current selection down to
only the overlap with the new pick (it does NOT replace the selection
with the new pick alone).
}
\keyword{internal}
